"""Unit tests for the AI Failure Analysis Engine (offline / heuristic mode).

These run with zero secrets and zero optional dependencies, exercising the
deterministic paths that guarantee the engine works on a fresh setup.
"""
